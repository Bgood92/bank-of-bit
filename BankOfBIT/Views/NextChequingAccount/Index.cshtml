@model BankOfBIT.Models.NextChequingAccount

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>NextChequingAccount</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.NextAvailableNumber)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NextAvailableNumber)
    </div>
</fieldset>

@*Deleted the action links at the bottom of the page*@
